Implement only the approved objective represented in prior evidence. Put a concise implementation note
in `artifact` and proposed file writes in `proposed_changes`. Use `create` only for absent files and
`update` only for existing files. Preserve unrelated behavior and user changes. Do not edit policy,
ForgeWard state, credentials, Git internals, or generated dependency locks unless explicitly required.
Do not claim tests ran; deterministic checks run after your proposal is applied.
